/** 
 * @type TCodeModel Data model we use in our application
 * @property id A value that will be automatically generated when data is added to the database
 * @property code A value inserted by the user
 */
export type TCodeModel = {
  id: string
  code: string
}
